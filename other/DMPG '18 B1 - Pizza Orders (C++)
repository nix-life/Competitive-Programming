#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int p, c, v;
    cin >> p >> c >> v;

    cout << (p + 2) / 3 + (c + 2) / 3 + (v + 2) / 3;
    return 0;
}
